extends ../layout/admin

block styles
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
    meta(name="csrf-token" content= csrfToken)
block contenido
    .py-10
        h1.text-4xl.my-10.font-extrabold.text-center.text-action Bienes
            span.font-normal Raíces  
        h2.text-center.text-2xl.text-secondaryText= pagina

        p.text-xl-font-bold.text-center.my-10.text-red-700= mensaje
            span.text-xl-font-bold.text-center.my-10.text-midnightGreen= correo
                span.text-xl-font-bold.text-center.my-10.text-red-700=mensajeDos

        div(class="bg-formBackground shadow py-8 px-4 rounded mx-auto max-w-4xl my-10 md:px-10")
            form(id="imagen" class="dropzone border-dashed border-2 w-full min:h-96 h-auto rounded flex-col justify-center items-center" method="POST" action=`/auth/fotoPerfil/${usuario.id}` data-context="registro")
                input(type="hidden" name="_csrf" value= csrfToken)
            button(class=" mt-5 w-full py-3 bg-darkCyan hover:bg-midnightGreen rounded text-white font-bold uppercase cursor-pointer" type="button" id="publicar") Subir foto de Perfil    


block scripts 
    script(src="/js/agregarImagen.js")